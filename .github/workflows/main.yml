name: Build Hub

on:
  pull_request:
    paths-ignore:
      - '**.md'
  push:
    paths-ignore:
      - '**.md'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v4
        with:
          go-version: '1.25'
          cache: true
      - run: make cmd

  test-unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v4
        with:
          go-version: '1.25'
          cache: true
      - run: make test

  test-binding:
    needs:
      - test-unit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: konveyor/tackle2-seed
      - run: |
          mkdir -p /tmp/seed
          cp -r resources/* /tmp/seed
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v4
        with:
          go-version: '1.25'
          cache: true
      - run: |
          make vet
          echo "=== Starting server for test-binding ==="
          DISCONNECTED=1 make run > /tmp/hub-binding.log 2>&1 &
          SERVER_PID=$!

          echo "=== Waiting up to 90 seconds ==="
          for i in {1..90}; do
            if grep -q "listening" /tmp/hub-binding.log 2>/dev/null; then
              echo "Server ready after $i seconds"
              break
            fi
            sleep 1
            echo -n "."
          done
          echo ""

          echo "=== Server logs ==="
          tail -50 /tmp/hub-binding.log

          HUB_BASE_URL=http://localhost:8080 make test-binding

  test-api:
    needs:
    - test-unit
    runs-on: ubuntu-latest
    env:
      DISCONNECTED: 1
      HUB_BASE_URL: http://localhost:8080
      DB_PATH: /tmp/hub.db
    steps:
      - uses: actions/checkout@v4
        with:
          repository: konveyor/tackle2-seed
      - run: |
          mkdir -p /tmp/seed
          cp -r resources/* /tmp/seed
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v4
        with:
          go-version: '1.25'
          cache: true
      - run: |
          rm -f $DB_PATH
          echo "=== Go module cache status ==="
          ls -la $(go env GOMODCACHE)/cache/lock || echo "No locks"

          echo "=== Starting server with verbose output ==="
          stdbuf -oL -eL bash -x -c 'make run' > /tmp/hub.log 2>&1 &
          SERVER_PID=$!
          echo "Server PID: $SERVER_PID"

          echo "=== Waiting up to 90 seconds for server startup ==="
          for i in {1..90}; do
            sleep 1
            if grep -q "listening" /tmp/hub.log 2>/dev/null; then
              echo "Server started after $i seconds"
              break
            fi
            echo -n "."
          done
          echo ""

          echo "=== Server process status ==="
          ps aux | grep -E "PID|$SERVER_PID" || echo "Process not found"

          echo "=== Server logs ==="
          cat /tmp/hub.log || echo "No log file"

          echo "=== Process tree ==="
          pstree -p $SERVER_PID || ps --forest -g $(ps -o pgid= -p $SERVER_PID | tr -d ' ')

          echo "=== Check if go run is actually running ==="
          ps aux | grep "go run" | grep -v grep || echo "No go run process found"

          echo "=== Running tests ==="
          make test-api

  build-image:
    needs:
    - build
    - test-unit
    - test-binding
    runs-on: ubuntu-latest
    env:
      IMG: ttl.sh/konveyor-hub-${{ github.sha }}:2h
    steps:
      - uses: actions/checkout@v4
      - run: make docker-build
      - run: docker push ${IMG}

  test-integration:
    needs: build-image
    uses: konveyor/ci/.github/workflows/global-ci-bundle.yml@main
    with:
      tackle_hub: ttl.sh/konveyor-hub-${{ github.sha }}:2h
      api_hub_tests_ref: ${{ github.ref }}
